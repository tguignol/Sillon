# Revue complète Sillon — findings restants (2026-06-27)

Audit multi-agents (bugs, sécurité, concurrence, perf, qualité) sur iOS/iPadOS/macOS.

**Les 12 findings HAUTE sévérité ont tous été corrigés** (commits « Revue (lot 1→6) » + macOS quit) ou traités (ATS volontairement conservé pour ne pas casser les serveurs http locaux, avec avertissement http ajouté à l'ajout de serveur).

Ci-dessous les findings **moyenne** et **basse** restants (non vérifiés adversarialement → certains peuvent être des faux positifs ; à confirmer avant correction). Plusieurs gagneront à être traités/validés lors des tests sur appareil.


## Moyenne (27)

- **[plateforme]** `Sillon/App/RootTabView.swift:27` — La rotation iPad reconstruit toute la hierarchie de navigation (perte d'etat)
  - Sur iPad, le choix entre SidebarRootView (paysage) et tabRoot (portrait) est pilote par un GeometryReader comparant largeur/hauteur. A chaque rotation, SwiftUI bascule entre deux sous-arbres de vues completement differents : la pile de navigation, la selection de barre laterale, la position de defil
  - *Fix proposé* : Conserver une seule hierarchie persistante. Utiliser NavigationSplitView (SidebarRootView) pour tout iPad quel que soit le format (il gere nativement le repli de colonne en portrait et preserve l'etat lors des rotations), ou deplacer l'etat
- **[bug]** `Sillon/Player/PlayerController.swift:464` — seek/seekGapless ne clampe pas la position à la durée : un seek au-delà de la fin laisse un état incohérent — ✅ CORRIGÉ (lot 4)
  - seek(to:) ne borne pas `seconds`. changePlaybackPositionCommand (ligne 1176, écran verrouillé/AirPods) passe e.positionTime directement à seek, et restoreLastSession (ligne 1305) passe state.position d'un fichier potentiellement plus court qu'avant. Dans seekGapless, `frame = seconds*sampleRate` peu
  - *Fix proposé* : Clamper dès seek : `let s = min(max(0, seconds), max(0, duration))`. Et dans le cas remaining<=0, recharger proprement (loadCurrent) ou laisser isPlaying cohérent plutôt que de laisser un audioFile sans segment planifié.
- **[securite]** `Sillon/Player/PlayerController.swift:707` — Le cache de flux temporaire n'est jamais purgé et peut diverger pour un même morceau
  - cacheURL nomme le fichier `sanitize(track.id).format`. À la ligne 693 on retourne le cache existant sans vérification de fraîcheur (un re-transcodage côté serveur ne serait jamais repris). Surtout, le répertoire SillonStreamCache dans temporaryDirectory n'est purgé nulle part dans ce contrôleur : il
  - *Fix proposé* : Mettre en place une purge bornée (LRU ou taille max) du répertoire SillonStreamCache, et déplacer le cache dans Caches/ plutôt que temporaryDirectory pour un comportement de purge plus prévisible.
- **[performance]** `Sillon/Player/PlayerController.swift:537` — context.save() synchrone sur le MainActor à chaque transition de morceau
  - markCurrentAlbumPlayed (appelé à chaque lecture, chaque advanceGapless ligne 905, chaque beginCrossfade ligne 982) fait `try? context.save()` sur le mainContext, donc sur le MainActor. Un save SwiftData synchrone à chaque transition de piste (et en rafale lors d'un skip rapide dans la file) bloque l
  - *Fix proposé* : Éviter le save si album.lastPlayedDate est déjà récent (< quelques secondes), ou déporter la mise à jour de lastPlayedDate dans un contexte d'arrière-plan dédié.
- **[bug]** `Sillon/Player/PlayerController.swift:921` — Transition gapless cassée (blanc + re-fetch) quand le morceau suivant a une fréquence d'échantillonnage différente
  - scheduleNextGapless (ligne 920-921) ne pré-planifie le suivant QUE si `file.processingFormat.sampleRate == sampleRate`. Si les fréquences diffèrent (44,1 vs 48 kHz, très courant), nextFile reste nil. En fin de morceau, handleTrackEnded (ligne 880) voit `nextFile == nil` et tombe dans handlePlaybackE
  - *Fix proposé* : Pré-résoudre/pré-télécharger le suivant même si la fréquence diffère (au moins pour amortir la latence réseau), et n'utiliser la garde de sample-rate que pour décider planification-sur-le-même-nœud vs rechargement-avec-reconnexion, sans re-
- **[bug]** `Sillon/Player/AudioSpectrumAnalyzer.swift:60` — Le spectre ne se met jamais à jour si le tap fournit des buffers plus courts que fftSize
  - `bufferSize: AVAudioFrameCount(fftSize)` (l.46) n'est qu'une indication : CoreAudio peut livrer des tampons plus petits (fréquent selon le matériel / la session). Le guard `Int(buffer.frameLength) >= fftSize` (l.60) abandonne alors silencieusement chaque buffer, et l'analyseur ne publie plus jamais 
  - *Fix proposé* : Accumuler les échantillons dans un ring buffer interne jusqu'à atteindre `fftSize`, puis lancer la FFT — au lieu d'exiger un buffer unique de taille fftSize. À défaut, demander une taille de tap plus petite et zéro-padder, ou au minimum log
- **[bug]** `Sillon/Player/ReplayGain.swift:34` — Gain et peak choisis indépendamment : protection anti-clipping appliquée avec un peak non apparié au gain — ✅ CORRIGÉ (lot 6)
  - En mode `.album`, `gainDB` et `peak` sont résolus par deux chaînes de repli séparées (l.34-35). Exemple concret : `albumGain` est présent mais `albumPeak` est nil tandis que `trackPeak` existe → on applique le gain d'album mais avec le peak de la *piste*. Le clip-protection (`factor ≤ 1/peak`, l.47)
  - *Fix proposé* : Apparier gain et peak dans la même branche de repli : choisir le couple (gain, peak) cohérent ensemble. Ex. : `if let g = albumGain { gainDB = g; peak = albumPeak } else if let g = albumRelGain { gainDB = g; peak = albumRelPeak } else if le
- **[bug]** `Sillon/Networking/Jellyfin/JellyfinProvider.swift:147` — Curseur de delta basé sur l'horloge CLIENT (.now) → updates manqués en cas de dérive d'horloge
  - `newSyncCursor` est calculé avec `ISO8601DateFormatter().string(from: .now)` : c'est l'heure de l'appareil au moment de la synchro, alors que le filtre `MinDateLastSaved` (ligne 123-126) est évalué côté serveur contre les `DateLastSaved` du serveur. Si l'horloge du client est en avance sur celle du 
  - *Fix proposé* : Dériver le curseur d'une source serveur plutôt que de `.now` : par exemple prendre le max des `DateLastSaved`/`DateCreated` réellement renvoyés par le serveur dans cette passe, ou récupérer l'heure serveur. À défaut, soustraire une marge de
- **[bug]** `Sillon/Networking/Jellyfin/JellyfinProvider.swift:287` — extraQuery peut dupliquer/écraser des paramètres de pagination critiques
  - `fetchItems` ajoute `extraQuery` APRÈS les paramètres fixes sans déduplication (ligne 287). Les appelants passent des clés arbitraires : `MinDateLastSaved`, `SearchTerm`, `Filters`. Aujourd'hui aucune ne collisionne, mais le contrat est fragile : si un appelant passait `Limit`, `StartIndex`, `Recurs
  - *Fix proposé* : Fusionner explicitement : construire un dictionnaire des paramètres de base puis y appliquer `extraQuery` (les clés réservées de pagination étant protégées), ou documenter/asserter que `extraQuery` ne doit jamais contenir StartIndex/Limit/R
- **[securite]** `Sillon/Networking/Subsonic/SubsonicProvider.swift:299` — Aucune validation du schéma d'URL : secrets Subsonic potentiellement envoyés en http://
  - makeAuthenticatedURL construit baseURL.appending(path:) sans vérifier que baseURL est en https. u/t/s sont des paramètres de query (l.303-308) : sur un serveur http:// ils transitent en clair. Le provider est construit directement depuis server.baseURL (ServerProviderFactory l.13-14) sans contrôle. 
  - *Fix proposé* : Vérifier baseURL.scheme == "https" dans makeAuthenticatedURL (ou à la création du provider) et, sinon, soit lever une erreur, soit exiger un opt-in explicite « j'accepte le HTTP non chiffré » côté ServerAccount, propagé jusqu'ici.
- **[bug]** `Sillon/Networking/Subsonic/SubsonicProvider.swift:81` — Fenêtre de course sur le curseur de syncDelta : albums ajoutés pendant le scan perdus définitivement
  - syncDelta calcule newSyncCursor = ISO8601DateFormatter().string(from: .now) APRÈS la fin de fetchAllAlbumsAndTracks (l.71 puis l.81). Tout album dont `created` tombe entre le début de la pagination et ce .now final ne sera pas remonté à ce cycle (il a pu être paginé après, ou pas du tout selon le tr
  - *Fix proposé* : Capturer `let cursorNow = Date.now` au tout début de syncDelta et l'utiliser comme newSyncCursor, de sorte que la prochaine delta re-couvre la fenêtre du scan (les doublons éventuels étant idempotents côté upsert). Idem dans LocalFilesProvi
- **[bug]** `/Users/thomaskohler/_DEV/Github/Sillon/Sillon/Sync/LibrarySyncService.swift:64` — Curseur delta non avance si le provider renvoie newSyncCursor == nil
  - En mode delta, le curseur n'est mis a jour que `if let cursor = delta.newSyncCursor`. Si un provider renvoie `newSyncCursor == nil` (ex. endpoint qui ne fournit pas de curseur, ou reponse partielle), `server.syncCursor` reste fige sur l'ancienne valeur. La synchro suivante redemandera donc le MEME i
  - *Fix proposé* : Definir un comportement de repli explicite : si `delta.newSyncCursor == nil` mais que le delta a reussi, avancer le curseur a l'instant present : `server.syncCursor = delta.newSyncCursor ?? ISO8601DateFormatter().string(from: .now)` (en cap
- **[performance]** `/Users/thomaskohler/_DEV/Github/Sillon/Sillon/Sync/LibrarySyncService.swift:106` — Scan SwiftData global de tous les albums (toutes sources) a chaque prefetch
  - `prefetchArtwork` charge `context.fetch(FetchDescriptor<Album>())` SANS predicat, donc TOUS les albums de TOUS les serveurs en memoire, puis filtre en Swift par `$0.server?.id == serverID`. Combine avec `LibraryIndex.init` (lignes 338-340) qui fait la meme chose pour Artist/Album/Track, une seule sy
  - *Fix proposé* : Utiliser un FetchDescriptor avec predicat sur le prefixe d'identifiant composite (deja explique inutilisable via `starts(with:)` au commentaire ligne 330) OU stocker `serverID: UUID` en attribut indexe direct sur Album/Track/Artist et filtr
- **[securite]** `/Users/thomaskohler/_DEV/Github/Sillon/Sillon/Sync/LibrarySyncService.swift:140` — Pochettes telechargees via URLSession.shared sans verification du schema (HTTP en clair accepte)
  - Le prefetch fait `URLSession.shared.data(from: target.url)` sur l'URL renvoyee par `provider.coverArtURL(...)`. Pour un serveur configure en HTTP (Navidrome/Jellyfin auto-heberge sur http://), cette URL contient typiquement le jeton d'authentification (Subsonic met `t`/`s`/`u` en query string, Jelly
  - *Fix proposé* : Verifier `target.url.scheme == "https"` avant le telechargement, ou propager le choix HTTP comme un opt-in explicite (deja gere ailleurs pour la lecture). A minima, journaliser/avertir une fois quand des jetons transitent en HTTP, et ne jam
- **[qualite]** `/Users/thomaskohler/_DEV/Github/Sillon/Sillon/Models/DownloadTask.swift:13` — DownloadTask orphelin : lien Track par String, aucune cascade de suppression
  - `DownloadTask.trackID: String` référence `Track.id` mais SANS relation SwiftData. Quand un `Track` est supprimé (suppression d'un serveur → cascade ServerAccount→Artist→Album→Track via les `@Relationship(.cascade)`), les `DownloadTask` correspondants NE sont PAS supprimés : ils deviennent des lignes
  - *Fix proposé* : Soit transformer le lien en vraie relation `var track: Track?` avec `deleteRule: .nullify` (et purger les tâches dont `track == nil` au lancement), soit ajouter une étape de nettoyage dans `reconcileOnLaunch` qui supprime tout `DownloadTask
- **[bug]** `/Users/thomaskohler/_DEV/Github/Sillon/Sillon/Models/DownloadTask.swift:25` — Réassociation par urlSessionTaskIdentifier non fiable entre sessions
  - `urlSessionTaskIdentifier` (Int) sert à ré-associer une tâche système après relance (DownloadManager `reconcileOnLaunch` l.157-158 : `liveIDs.contains(tid)`). Or `URLSessionTask.taskIdentifier` n'est unique QUE au sein d'une même instance d'URLSession ; après recréation de la session au relancement 
  - *Fix proposé* : Ne pas se fier au taskIdentifier brut pour l'identité inter-session. Associer chaque tâche à un identifiant stable (ex. `trackID`) via `URLSessionTask.taskDescription` ou un mapping persistant, et faire la réconciliation sur cette clé stabl
- **[bug]** `Sillon/Downloads/DownloadManager.swift:77` — Re-enqueue après échec : ancienne tâche système non annulée (doublon)
  - enqueue crée systématiquement une nouvelle session.downloadTask(with:) (ligne 73) et réutilise/insère un record. Le guard ligne 62 ne bloque que les états downloaded/downloading/queued ; un morceau en .failed peut être re-enqueué alors qu'une tâche système précédente est encore vivante (échec applic
  - *Fix proposé* : Avant de créer la nouvelle tâche, annuler toute tâche système existante pour ce trackID : `session.getAllTasks { tasks in tasks.filter { Self.decode($0.taskDescription)?.trackID == trackID }.forEach { $0.cancel() } }`, puis programmer la no
- **[performance]** `Sillon/Downloads/DownloadManager.swift:222` — Fetchs SwiftData répétés à chaque callback de progression
  - updateProgress (appelé très fréquemment pendant un téléchargement via didWriteData) déclenche taskRecord(forTaskIdentifier:) qui fait un context.fetch avec prédicat à chaque callback, et met à jour Track via un second fetch (track(for:)). Pour plusieurs téléchargements parallèles ce sont des dizaine
  - *Fix proposé* : Throttler les mises à jour de progression (ne propager qu'au franchissement d'un delta, ex. >= 1%, ou via un timer) et/ou maintenir un cache en mémoire trackID -> DownloadTask peuplé à l'enqueue pour éviter les fetchs par prédicat à chaque 
- **[bug]** `Sillon/Downloads/DownloadFileLayout.swift:17` — Fallback baseDirectory vers temporaryDirectory : téléchargements perdus silencieusement
  - Si l'accès à Application Support (macOS) ou Documents (iOS) échoue, baseDirectory retombe sur fm.temporaryDirectory. Or temporaryDirectory n'est PAS le même dossier d'un lancement à l'autre et est purgeable par le système. Un morceau marqué .downloaded avec un localFileURLString sous /tmp peut dispa
  - *Fix proposé* : Ne pas utiliser temporaryDirectory comme racine persistante. En cas d'échec, propager l'erreur (faire de baseDirectory un throws) ou utiliser un chemin stable garanti, et stocker des chemins relatifs à la racine recalculée au runtime plutôt
- **[securite]** `Sillon/Views/Servers/AddServerView.swift:73` — Secrets orphelins en Keychain si la feuille est fermée autrement que par 'Annuler'
  - discardDraft() (qui appelle KeychainStore.deleteAll(for: draftServerID)) n'est invoqué QUE par le bouton 'Annuler' (ligne 49). Or sur iOS/iPadOS la feuille .sheet est fermable par glissement vers le bas (swipe-to-dismiss), et sur macOS par Échap, sans déclencher le bouton Annuler. Dans ce cas, si te
  - *Fix proposé* : Nettoyer le brouillon sur toute disparition de la vue, pas seulement via le bouton Annuler : ajouter `.onDisappear { if !viewModel.didSave { viewModel.discardDraft() } }` (en ajoutant un flag `didSave` mis à true dans save()), ou utiliser `
- **[securite]** `Sillon/ViewModels/AddServerViewModel.swift:109` — persistDraftCredentials() écrit les secrets en Keychain avant tout succès de connexion
  - testConnection() (ligne 113) appelle persistDraftCredentials() AVANT provider.authenticate(). Donc dès qu'on appuie sur 'Tester la connexion', le mot de passe/token/sel sont écrits en Keychain, même si l'authentification échoue ensuite (mauvais mot de passe, serveur injoignable, faute de frappe). Co
  - *Fix proposé* : Soit passer les secrets en mémoire au provider pour le test (sans Keychain), et ne persister qu'au save() réussi ; soit, a minima, en cas d'échec d'authenticate() dans le bloc catch (ligne 121), appeler KeychainStore.deleteAll(for: draftSer
- **[bug]** `Sillon/ViewModels/AddServerViewModel.swift:89` — didPickLocalFolder : le bookmark est créé hors de l'accès security-scoped sur macOS
  - Sur macOS le bookmark .withSecurityScope est créé (ligne 97) à l'intérieur du bloc startAccessingSecurityScopedResource/stopAccessingSecurityScopedResource, ce qui est correct. Mais le defer (ligne 93) arrête l'accès dès la sortie de la fonction, alors que le bookmark n'est résolu/consommé que bien 
  - *Fix proposé* : Sur iOS, ne pas faire dépendre tout l'échec du retour de startAccessingSecurityScopedResource : tenter la création du bookmark même si l'appel retourne false, ou utiliser `_ = url.startAccessingSecurityScopedResource()` puis traiter uniquem
- **[bug]** `/Users/thomaskohler/_DEV/Github/Sillon/Sillon/DesignSystem/Theme.swift:42` — placeholderGradient n'est PAS déterministe entre deux lancements (hashValue aléatoire) — ✅ CORRIGÉ (lot 4)
  - Le commentaire promet explicitement « Teinte déterministe dérivée du seed » et « un même album garde toujours la même couleur d'un écran à l'autre ». Or `String.hashValue` en Swift utilise une graine de hachage RANDOMISÉE par processus (hash randomization activée par défaut). La teinte est donc stab
  - *Fix proposé* : Remplacer `abs(seed.hashValue)` par un hash STABLE et reproductible, p.ex. une somme déterministe des octets UTF-8 : `let hue = Double(seed.utf8.reduce(5381) { ($0 &* 33) &+ UInt32($1) } % 360) / 360.0`. Tout hash non dépendant de la graine
- **[bug]** `Sillon/Player/PlayerController.swift:1220` — Glitch visuel : nouveau titre affiché avec l'ancienne pochette (ordre publish/clearCover)
  - Au changement de morceau, l'ordre est : `updateNowPlayingInfo()` appelle `publish(...)` qui fait `WidgetCenter.reloadAllTimelines()` (SillonWidget.swift:50/106 relit np-cover.dat) AVANT que `loadArtwork(for:)` n'appelle `clearCover()` (ligne 1220). Au moment du reload déclenché par `publish`, le fic
  - *Fix proposé* : Appeler `NowPlayingWidgetBridge.clearCover()` AVANT `publish(...)` lors d'un changement de morceau (ou faire en sorte que `publish` efface aussi la pochette quand le titre change), et idéalement ajouter un `WidgetCenter.shared.reloadAllTime
- **[bug]** `SillonWidget/SillonWidget.swift:185` — La progression « vivante » continue de compter après la fin du morceau si l'app est suspendue
  - La timeline utilise `policy: .never` (ligne 102) : le widget ne se recharge JAMAIS de lui-même, il compte uniquement sur `WidgetCenter.reloadAllTimelines()` déclenché par l'app. En lecture (`liveProgress`), `Text(timerInterval: virtualStart...virtualEnd)` (ligne 185) et le `ProgressView(timerInterva
  - *Fix proposé* : Ajouter une entrée de timeline supplémentaire à l'instant `virtualEnd` (au lieu de `policy: .never`) qui repasse en état statique/pause, ou utiliser `policy: .after(entry.virtualEnd)` pour forcer un recalcul à la fin du morceau. Cela garant
- **[concurrence]** `Sillon/Views/Player/SpectrumRingView.swift:60` — history mutée dans onChange depuis le rendu de Canvas sans garantie explicite de cadence/MainActor
  - onChange(of: levels) accumule history.append(new) et coupe à 18 éléments. levels est mis à jour par PlayerController (@Observable) ~à chaque frame audio/affichage. À très haute cadence d'updates de levels, history (jusqu'à 18 tableaux de 48 Float) est recopié en permanence ; surtout, l'accumulation 
  - *Fix proposé* : N'alimenter history que lorsque le style en a besoin (style == .cascade), p.ex. guard style == .cascade else { return } au début du onChange. Cela supprime le coût pour tous les autres styles tout en gardant la cascade prête après un court 
- **[securite]** `Sillon/Views/Shared/ArtworkLoader.swift:38` — Repli renvoyant une URL distante porteuse d'identifiants à AsyncImage (cache disque) — ✅ CORRIGÉ (lot 6)
  - En cas d'echec du telechargement (catch), localCoverURL renvoie l'URL distante `remote`. Or cette URL embarque les identifiants : pour Jellyfin un `api_key` en query (cf. JellyfinProvider.coverArtURL ligne 183), pour Subsonic le jeton+sel via makeAuthenticatedURL. CoverArtView (ligne 53) affecte ce 
  - *Fix proposé* : Ne pas renvoyer l'URL credentialisee comme repli. Retourner `nil` sur echec (l'UI affiche son placeholder, comme le decrit deja le commentaire de la fonction) : remplacer `return remote` par `return nil`. Si un retry reseau via AsyncImage e

## Basse (38)

- **[qualite]** `Sillon/App/SidebarRootView.swift:31` — selection nil affiche Accueil sans le surligner dans la barre laterale
  - section(selection ?? .accueil) affiche silencieusement HomeView quand selection vaut nil, mais la List(selection:) ne montre alors aucune entree selectionnee. L'utilisateur voit le contenu Accueil tandis que la barre laterale n'a rien de surligne : etat incoherent. selection peut devenir nil dans ce
  - *Fix proposé* : Garantir que selection n'est jamais nil : remplacer @State private var selection: SidebarSection? = .accueil par un binding non optionnel, ou ajouter un onChange(of: selection) { _, new in if new == nil { selection = .accueil } } pour resel
- **[qualite]** `Sillon/App/SidebarRootView.swift:48` — Fermeture inattendue du lecteur quand selection se re-emet a l'identique
  - onChange(of: selection) force showPlayer = false a chaque emission de selection. Si selection est reaffectee a la meme valeur (clic repete sur la section courante) ou transite par nil, le lecteur plein format se referme alors que l'utilisateur n'a pas reellement change de section.
  - *Fix proposé* : Ne fermer le lecteur que sur un vrai changement de section : onChange(of: selection) { old, new in if new != old { showPlayer = false } } (ici old != new est deja garanti par onChange, mais proteger explicitement contre nil : if let new, ne
- **[performance]** `Sillon/App/RootTabView.swift:44` — @Query global sur tous les ServerAccount + filtre recalcule a chaque rendu
  - RootTabView declenche @Query private var servers: [ServerAccount] (fetch global de tous les serveurs) uniquement pour calculer servers.filter(\.isActive).count > 1. Le filtre alloue un tableau a chaque evaluation du body, et toute modification d'un ServerAccount (ex. mise a jour de date de sync) rec
  - *Fix proposé* : Restreindre le @Query a l'essentiel via un predicate (#Predicate { $0.isActive }) puis tester servers.count > 1, ou exposer hasMultipleServers via un modele @Observable mis a jour uniquement lors des changements d'activation, plutot qu'un f
- **[concurrence]** `Sillon/Player/PlayerController.swift:569` — Task de chargement non annulée : isLoading et téléchargements concurrents sur skips rapides
  - loadCurrent est lancé via des `Task { await loadCurrent(...) }` détachés (play/next/previous/jump) jamais stockés ni annulés. Sur des next() rapides, plusieurs loadCurrent se chevauchent. La planification est protégée par scheduleGeneration et la pochette par artworkToken (pas de crash), mais des té
  - *Fix proposé* : Conserver la Task de chargement courante (@ObservationIgnored private var loadTask) et l'annuler en tête de loadCurrent/next/jump pour sérialiser, et ne remettre isLoading à false que si la génération courante correspond.
- **[concurrence]** `Sillon/Player/PlayerController.swift:1083` — seek après loadCurrent en crossfade peut laisser le deck inactif à moitié préparé (race avec prepareNextDeck)
  - refreshCrossfade (1081-1084) et restoreLastSession (1303-1306) font `await loadCurrent(...)` puis `seek(to: pos)`. En crossfade, startCrossfadeLoad lance `Task { await prepareNextDeck }` non attendue. seek -> seekCrossfade -> abortCrossfade arrête idleDeck.player et remet deck.file/trackIndex à nil 
  - *Fix proposé* : Après le seek de restauration/refresh en crossfade, relancer explicitement prepareNextDeck(generation:), ou sérialiser : ne déclencher la préparation du deck inactif qu'après la fin du seek.
- **[bug]** `Sillon/Player/AudioSpectrumAnalyzer.swift:67` — Forme d'onde calculée sur le 1er canal sans vérifier le nombre de canaux disponibles
  - `samples = channelData[0]` (l.61) puis `samples[i * stride]` (l.67) supposent au moins un canal et un layout non-entrelacé. C'est le cas pour `mainMixerNode` aujourd'hui, mais si le format du tap devenait entrelacé stéréo, `channelData[0]` ne contiendrait que les échantillons entrelacés et l'indexat
  - *Fix proposé* : Vérifier `buffer.format.isInterleaved == false` (et `channelData != nil`) avant de traiter, et documenter/asserter l'attente d'un format non-entrelacé. Sinon gérer explicitement le désentrelacement.
- **[performance]** `Sillon/Player/PlayerController.swift:819` — Création d'une Task @MainActor par tampon audio (~43/s) depuis le thread temps réel
  - Le callback du tap exécute `Task { @MainActor in self?.applySpectrum(...) }` à chaque buffer (≈ toutes les ~23 ms à 44,1 kHz / 1024 frames). On alloue donc une Task et on planifie un hop MainActor des dizaines de fois par seconde en continu pendant toute la lecture, churn inutile d'allocations/sched
  - *Fix proposé* : Throttler la publication (ex. au plus ~30 fps) ou utiliser `MainActor.assumeIsolated`/un `AsyncStream` consommé côté MainActor, plutôt qu'une nouvelle Task par buffer. Idéalement ne pas faire d'allocation dans le callback temps réel : dépos
- **[qualite]** `Sillon/Networking/Jellyfin/JellyfinProvider.swift:176` — coverArtURL avale les erreurs d'auth et peut renvoyer une URL sans api_key
  - Ligne 176 : `_ = try? await authenticate()` ignore silencieusement tout échec d'authentification. Ligne 183 : `api_key` n'est ajouté que `if let token = cachedToken`. Si l'auth échoue (réseau, identifiants), la fonction renvoie quand même une URL de pochette SANS jeton, qui sera rejetée (401) par le
  - *Fix proposé* : Soit utiliser `try await ensureAuthenticated()` et propager l'erreur, soit, si le silence est voulu, retourner `nil` quand `cachedToken == nil` après tentative, plutôt qu'une URL non authentifiée vouée au 401.
- **[securite]** `Sillon/Networking/Jellyfin/JellyfinProvider.swift:361` — Le corps de réponse brut est capturé dans ProviderError.unexpectedResponse
  - `validate` met `String(data: data, encoding: .utf8)` dans `.unexpectedResponse(body:)` pour tout code non-2xx hors 401. Cet appel est notamment exécuté pour la réponse d'`AuthenticateByName` (ligne 61), dont le corps d'erreur peut refléter des éléments de la requête. Le `errorDescription` (ProviderM
  - *Fix proposé* : Tronquer/assainir le body avant de le stocker (ex. limiter à N octets et retirer toute paire ressemblant à pw/token/api_key), ou ne conserver qu'un résumé. À défaut, documenter que cet Error ne doit jamais être loggé en entier.
- **[bug]** `Sillon/Networking/Subsonic/SubsonicProvider.swift:192` — Offset de paroles appliqué deux fois quand le serveur applique déjà l'offset global
  - Pour chaque ligne, timeSeconds = start/1000 + offsetSeconds (l.190-192), où offsetSeconds vient de structured.offset. La sémantique OpenSubsonic de `offset` est un décalage global ; mais certains serveurs renvoient déjà des `start` ajustés, d'autres non. Surtout, si offset est volumineux et qu'une l
  - *Fix proposé* : Vérifier la sémantique offset du serveur cible (Navidrome) : si le serveur applique déjà l'offset, ne pas le ré-additionner. À défaut, rendre l'addition de l'offset conditionnelle/documentée et testée contre un fichier .lrc connu.
- **[performance]** `Sillon/Networking/Subsonic/SubsonicProvider.swift:50` — fetchLibrary : N+1 requêtes séquentielles pour les playlists et les détails d'albums
  - fetchLibrary fait un getPlaylist par playlist (boucle l.49-53) puis fetchAllAlbumsAndTracks fait un getAlbum par album (l.154-161), tout séquentiellement avec await. Pour une bibliothèque de quelques milliers d'albums c'est plusieurs milliers d'allers-retours HTTP en série → synchro initiale très le
  - *Fix proposé* : Introduire une parallélisation bornée (ex. withThrowingTaskGroup avec un sémaphore de concurrence ~4-8) pour les getAlbum/getPlaylist, en conservant l'ordre via un dictionnaire indexé par id. Sans dépasser une limite raisonnable pour ne pas
- **[bug]** `Sillon/Networking/Local/LocalFilesProvider.swift:191` — Collision d'identifiants d'album entre artistes/albums homonymes dans des dossiers différents
  - albumID = "local-album:\(artistName)/\(albumTitle)" (l.191) et artistID = "local-artist:\(artistName)" (l.190) sont dérivés des noms (tags ou dossiers), pas des chemins. Deux dossiers distincts contenant des albums de même artiste+titre (ex. une réédition, ou des tags incohérents) seront fusionnés e
  - *Fix proposé* : Soit dériver l'albumID du chemin du dossier parent (canonique et unique) plutôt que de noms libres, soit normaliser/échapper les composants. Au minimum, documenter que le regroupement local repose sur les noms et peut fusionner des homonyme
- **[qualite]** `Sillon/Networking/Subsonic/SubsonicProvider.swift:104` — coverArtURL avale toute erreur via try? et renvoie nil sans distinction (URL invalide vs absence de pochette)
  - coverArtURL fait `return try? makeAuthenticatedURL(...)` (l.104) : si les identifiants manquent (missingCredentials), si l'URL est invalide, etc., on renvoie silencieusement nil, indistinct du cas légitime « pas de pochette ». L'appelant (ArtworkLoader l.50, PlayerController l.1226) ne peut pas diff
  - *Fix proposé* : Laisser makeAuthenticatedURL propager throw (la signature est déjà `throws`), ou au minimum ne convertir en nil que l'absence de coverArtPath, pas les erreurs d'auth/URL. Ainsi un échec d'identifiants remonte au lieu d'être masqué.
- **[bug]** `/Users/thomaskohler/_DEV/Github/Sillon/Sillon/Sync/LibrarySyncService.swift:238` — sortName ecrase a chaque upsert : un sortName serveur absent efface une valeur precedente valide
  - A la creation (ligne 231), `sortName: remote.sortName` est passe tel quel (peut etre nil). A la mise a jour (ligne 238), `artist.sortName = remote.sortName ?? remote.name`. L'incoherence : si une premiere synchro fournit un `sortName` valide puis une synchro delta ulterieure renvoie un `RemoteArtist
  - *Fix proposé* : Ne pas ecraser un sortName existant par un fallback : `if let s = remote.sortName { artist.sortName = s }` — ou conserver l'ancienne valeur quand le delta n'en fournit pas. Harmoniser la valeur par defaut entre creation et mise a jour.
- **[bug]** `/Users/thomaskohler/_DEV/Github/Sillon/Sillon/Sync/LibrarySyncService.swift:267` — Relation artist/album non effacee si remote.artistID/albumID devient nil
  - Ligne 267 (`if let artistID = remote.artistID { album.artist = index.artist(artistID) }`) et ligne 304 (idem pour album) : la relation n'est mise a jour QUE si l'ID distant est present. Si un delta ulterieur signale qu'un morceau n'appartient plus a un album (ou un album n'a plus d'artiste), `remote
  - *Fix proposé* : Distinguer les deux cas : mettre a jour la relation uniquement quand l'index resout l'ID (`if let id = remote.artistID, let a = index.artist(id) { album.artist = a }`), et decider explicitement du comportement quand `remote.artistID == nil`
- **[performance]** `/Users/thomaskohler/_DEV/Github/Sillon/Sillon/Library/Deduplication.swift:35` — Pré-filtre dédup caseInsensitive en sous-chaîne : sur-récupération sur titres courts
  - Avec `caseInsensitive: true`, le pré-filtre utilise `localizedStandardContains(title)` (l.36-38 trackCopies, l.23-25 albumCopies, l.48-50 artistCopies). C'est une recherche de SOUS-CHAÎNE non indexée : pour un titre court fréquent (ex. « Live », « Intro », un artiste « Air »), le `fetch` ramène pote
  - *Fix proposé* : Conserver le pré-filtre exact `== title` indexé même en mode caseInsensitive, et ne traiter la divergence de casse/diacritiques qu'au post-filtre `DedupKey` (qui normalise déjà). Si la divergence de casse côté store doit vraiment être rattr
- **[bug]** `/Users/thomaskohler/_DEV/Github/Sillon/Sillon/Models/EQSettings.swift:55` — precondition sur bandCount : crash dur si valeur hors plage
  - `init(bandCount:)` utilise `precondition((6...12).contains(bandCount), ...)`. Une `precondition` plante l'app en production (non désactivable hors `-Ounchecked`). Si un jour `bandCount` provient d'une source dynamique (réglage importé, valeur migrée, futur appel), une valeur hors 6...12 fait crasher
  - *Fix proposé* : Remplacer la `precondition` par un clamp défensif : `let n = min(12, max(6, bandCount))` et utiliser `n` pour initialiser `bandCount` et `gainsDB`, garantissant un état toujours valide sans crash.
- **[qualite]** `Sillon/Downloads/DownloadManager.swift:196` — Échecs de sauvegarde SwiftData systématiquement avalés (try?)
  - Tous les enregistrements du contexte utilisent `try? context.save()` (lignes 92, 118, 129, 168, 196, 205) et tous les fetchs utilisent `try?`. Une erreur de persistance (disque plein, schéma) est silencieusement ignorée : l'état affiché (downloaded/failed) peut diverger de l'état persisté sans aucun
  - *Fix proposé* : Au minimum logger l'erreur de save (sans données sensibles) via le logging applicatif, et envisager de repasser le record en .failed avec un message utilisateur si la persistance échoue de façon répétée.
- **[performance]** `Sillon/Downloads/DownloadManager.swift:99` — enqueueAlbum sérialise les résolutions d'URL réseau
  - enqueueAlbum boucle en `await enqueue(track)` séquentiellement ; chaque enqueue fait un `await provider.downloadURL(...)` potentiellement réseau. Pour un album de 15+ titres, la mise en file se fait morceau par morceau, retardant le démarrage des derniers téléchargements et bloquant la progression d
  - *Fix proposé* : Les téléchargements eux-mêmes sont en background (parallèles), donc l'impact est limité, mais on peut paralléliser la phase de résolution d'URL/insertion via un TaskGroup borné, ou résoudre les URLs en lot, pour amorcer toutes les tâches pl
- **[qualite]** `Sillon/Views/Servers/ServerListView.swift:30` — Activation/désactivation d'un serveur : échec de save() silencieusement ignoré
  - onSetActive fait `server.isActive = isActive; try? modelContext.save()` (lignes 29-31). Le try? avale toute erreur de persistance : si le save échoue, le toggle reste visuellement basculé mais l'état n'est pas persisté, et l'utilisateur n'en sait rien. Même remarque pour onMove (ligne 45) et la tâch
  - *Fix proposé* : Au minimum logguer l'erreur (sans secret) en cas d'échec, ou remonter une alerte. Ex. : `do { try modelContext.save() } catch { /* log non sensible + éventuellement revert isActive */ }`.
- **[qualite]** `Sillon/Security/KeychainStore.swift:18` — OSStatus exposé tel quel dans le message d'erreur utilisateur
  - unexpectedStatus(OSStatus) formate le code brut dans 'Erreur Keychain (code %d).'. Ce n'est pas une fuite de secret (le code OSStatus n'est pas un identifiant), mais le message est peu actionnable pour l'utilisateur et %d sur un OSStatus (Int32) est correct ici. Point mineur : SecItemDelete dans sav
  - *Fix proposé* : Optionnel : mapper les OSStatus courants (errSecInteractionNotAllowed, errSecAuthFailed) vers des messages explicites, et utiliser SecCopyErrorMessageString(status, nil) pour enrichir le diagnostic sans exposer de secret.
- **[performance]** `/Users/thomaskohler/_DEV/Github/Sillon/Sillon/Player/NowPlayingWidgetBridge.swift:50` — reloadAllTimelines() appelé à chaque publish ET à chaque writeCover
  - `publish(...)` est invoqué par PlayerController à chaque changement d'état (lecture/pause, seek, changement de morceau, favori) et appelle `WidgetCenter.shared.reloadAllTimelines()`. Lors d'un changement de morceau, `publish` puis `writeCover` (ligne 59) déclenchent deux reloads quasi simultanés. Wi
  - *Fix proposé* : Coalescer les reloads : ne déclencher `reloadAllTimelines()` qu'une fois par changement significatif (p.ex. depuis l'appelant, après avoir écrit texte + cover), ou debouncer (ignorer un reload si le précédent date de < quelques secondes). É
- **[concurrence]** `/Users/thomaskohler/_DEV/Github/Sillon/Sillon/Localization/LanguageManager.swift:39` — Lecture/écriture de l'associated object du bundle redirigé sans synchronisation explicite
  - `LocalizedBundle.localizedString(forKey:)` lit l'associated object via `objc_getAssociatedObject` ; SwiftUI peut résoudre des chaînes localisées hors du main thread (diffing/rendu), alors que `apply(_:)` écrit l'association depuis le MainActor (SettingsRootView). `bundleKey` est marqué `nonisolated(
  - *Fix proposé* : Acceptable en l'état vu la rareté de l'opération. Si on veut durcir : forcer la réécriture de l'association et la reconstruction de l'UI (déjà fait via `.id(langue)`) de façon atomique sur le MainActor, et documenter que `apply` doit toujou
- **[qualite]** `SillonWidget/SillonWidgetLiveActivity.swift:53` — Live Activity = gabarit Xcode non modifié et non utilisé (code mort)
  - Tout le fichier est le template généré par Xcode : attributs `emoji`/`name`, UI « Hello 😀 », Dynamic Island « Leading/Trailing/Bottom ». Ce widget n'est PAS exposé dans `SillonWidgetBundle` (qui ne déclare que `SillonWidget()`), donc il ne tourne jamais. En plus, `.widgetURL(URL(string: "http://www.
  - *Fix proposé* : Soit supprimer entièrement `SillonWidgetLiveActivity.swift` (et `AppIntent.swift`, `SillonWidgetControl.swift`) tant qu'ils ne sont pas implémentés, soit, si une Live Activity « lecture en cours » est prévue, remplacer le contenu par les vr
- **[qualite]** `SillonWidget/SillonWidgetControl.swift:12` — Control Widget « Timer » = gabarit Xcode non utilisé (code mort)
  - `SillonWidgetControl` (toggle « Start Timer », `TimerConfiguration`, `StartTimerIntent`) est le template de minuteur généré par Xcode. `currentValue` renvoie toujours `isRunning = true` (commentaire « Check if the timer is running » jamais implémenté) et `perform()` ne fait rien. Comme il n'est pas 
  - *Fix proposé* : Supprimer ce fichier tant qu'aucun contrôle réel n'est prévu, ou le remplacer par un vrai contrôle (ex. play/pause) branché sur l'état partagé de l'App Group.
- **[qualite]** `SillonWidget/AppIntent.swift:11` — ConfigurationAppIntent = gabarit « Favorite Emoji » non utilisé
  - `ConfigurationAppIntent` avec son paramètre `favoriteEmoji` et la description « This is an example widget. » est le template Xcode. `SillonWidget` utilise une `StaticConfiguration` (pas d'AppIntentConfiguration), donc cet intent n'est référencé nulle part. Code mort.
  - *Fix proposé* : Supprimer `AppIntent.swift` (le widget n'a pas de paramètre configurable), ou le réutiliser si une vraie configuration est ajoutée.
- **[bug]** `SillonWidget/SillonWidget.swift:114` — anchor à 1970 si la clé np.anchor est absente → progression vivante aberrante
  - `Date(timeIntervalSince1970: d.double(forKey: "np.anchor"))` : `double(forKey:)` renvoie 0 si la clé manque, donnant anchor = 1er janvier 1970. Si `np.has == true` mais `np.anchor` jamais écrit (état partagé partiel, ex. migration ou écriture interrompue), `virtualStart = 1970 - elapsed` et le `Text
  - *Fix proposé* : Traiter une anchor invalide (ex. timeIntervalSince1970 < une borne raisonnable, ou clé absente via `object(forKey:) == nil`) comme « pas de progression vivante » : retomber sur l'affichage statique `fraction`/`mmss(elapsed)`. On peut lire `
- **[performance]** `SillonWidget/SillonWidget.swift:56` — Décodage image synchrone dans le provider sans borne mémoire (extension widget ~30 Mo)
  - `Shared.cover()` fait `Data(contentsOf:)` + `UIImage(data:)` (lignes 55-56) à chaque `read()`, exécuté dans `getTimeline`/`getSnapshot`. L'app écrit les données brutes téléchargées (PlayerController.swift:1232 `writeCover(data)`), donc la taille dépend de la réponse serveur (Jellyfin/Navidrome peuve
  - *Fix proposé* : Côté app, redimensionner/recompresser la pochette (ex. ≤ 200px JPEG) avant `writeCover`, et/ou côté widget utiliser un décodage downsamplé via `CGImageSourceCreateThumbnailAtIndex` avec `kCGImageSourceThumbnailMaxPixelSize` plutôt que `UIIm
- **[performance]** `Sillon/Views/Player/LyricsView.swift:67` — Paroles synchronisées : allocation + scan O(n) à chaque tick de currentTime sur le main actor
  - Le body de SyncedLyricsView lit player?.currentTime, donc il est ré-évalué à chaque tick de l'horloge (~plusieurs fois par seconde). À chaque ré-évaluation il construit un nouveau TrackLyrics(synced: true, lines: lines) (copie/alloue le tableau de lignes) puis appelle activeLineIndex(at:) qui scanne
  - *Fix proposé* : Ne pas reconstruire TrackLyrics dans le body. Calculer l'index actif sans allocation, p.ex. une fonction statique/locale activeLineIndex(in: lines, at: time) qui parcourt le tableau existant, ou mémoriser un TrackLyrics stable (let lyrics =
- **[performance]** `Sillon/Views/Player/EQCurveView.swift:102` — drawCurve recalcule responseGain (O(bandes)) pour 121 points à chaque rendu du Canvas
  - drawCurve itère sur 0...120 (121 points) et pour chaque point appelle responseGain(at:) qui boucle sur toutes les bandes (jusqu'à 12) avec log2/exp. Soit ~121*12 = ~1450 appels à exp/log2 par rendu. Comme le Canvas est redessiné à chaque onChange (drag continu de poignée), cela s'exécute en boucle p
  - *Fix proposé* : Réduire le nombre de pas (p.ex. 60) suffit visuellement pour une courbe lissée ; ou pré-calculer log2(fc) par bande une fois hors de la boucle des points au lieu de le recalculer pour chaque f.
- **[plateforme]** `Sillon/Views/Player/PlayerView.swift:322` — Aucun sélecteur de sortie audio sur macOS (RoutePickerView gardé iOS-only)
  - RoutePickerView (AirPlay/Bluetooth) n'est compilé que sous #if os(iOS) (l.320-324 et l.331-343). Sur macOS, l'utilisateur n'a aucun moyen, depuis le lecteur, de choisir la sortie audio. Ce n'est pas un crash, mais une incohérence fonctionnelle entre plateformes (la sortie macOS dépend alors entièrem
  - *Fix proposé* : Documenter explicitement que macOS s'appuie sur la sortie système (commentaire), ou fournir un équivalent macOS (menu listant les sorties via Core Audio) si le contrôle de sortie depuis le lecteur est attendu sur Mac.
- **[qualite]** `Sillon/Views/Player/NowPlayingBar.swift:34` — Bouton play/pause du mini-lecteur sans état de chargement ni désactivation
  - Le bouton appelle player.togglePlayPause() sans tenir compte de player.isLoading. Pendant un chargement de piste, l'icône bascule play/pause alors que rien ne se passe encore, et un tap répété peut déclencher des bascules incohérentes. Le lecteur plein écran, lui, affiche un ProgressView et désactiv
  - *Fix proposé* : Refléter isLoading dans le mini-lecteur : afficher un petit ProgressView à la place de l'icône (ou la désactiver) quand player.isLoading est vrai, par cohérence avec PlayerView.
- **[qualite]** `Sillon/Views/Player/PlayerView.swift:229` — Plage du slider de progression figée à 0.1 quand la durée est inconnue/nulle
  - Le Slider de progression utilise in: 0...max(player.duration, 0.1). Tant que duration vaut 0 (piste en cours de chargement, flux sans durée connue), la plage est 0...0.1 : le curseur paraît collé tout à droite dès que currentTime dépasse 0.1, et un drag peut produire un seek(to:) vers une valeur qua
  - *Fix proposé* : Désactiver le Slider (.disabled(player.duration <= 0)) tant que la durée n'est pas connue, ou n'autoriser le scrub que lorsque player.duration > 0, pour éviter un curseur incohérent pendant le chargement.
- **[performance]** `Sillon/Views/Downloads/DownloadsView.swift:27` — Fetch SwiftData par ligne dans le ForEach des téléchargements
  - track(for:) (ligne 39) exécute un FetchDescriptor<Track> avec prédicat id == ... pour CHAQUE ligne, à chaque rendu de la liste (ligne 27, dans le ForEach). Sur une file de téléchargements importante, cela multiplie les requêtes ponctuelles sur le main thread à chaque redraw au lieu d'un seul fetch g
  - *Fix proposé* : Faire un seul fetch des Track dont l'id est dans l'ensemble des task.trackID (prédicat `ids.contains($0.id)`), construire un dictionnaire [String: Track] une fois, et l'utiliser pour résoudre chaque ligne ; ou exposer la relation Track depu
- **[qualite]** `Sillon/Views/Playlists/AddToPlaylistView.swift:26` — Incohérence de trim entre la validation et la création de playlist
  - Le bouton Créer est désactivé sur trimmingCharacters(in: .whitespaces) (ligne 26), mais PlaylistActions.create trim avec .whitespacesAndNewlines et retombe sur « Nouvelle playlist » si vide. Un nom composé uniquement de retours à la ligne passe la validation (non désactivé) puis crée une playlist no
  - *Fix proposé* : Utiliser .whitespacesAndNewlines dans la condition .disabled pour aligner la validation sur la logique de create().
- **[qualite]** `Sillon/Views/Shared/ArtworkCache.swift:43` — store() ignore l'echec d'ecriture mais renvoie l'URL comme si le fichier existait
  - `try? data.write(...)` avale toute erreur d'ecriture (disque plein, permissions, dossier purge entre-temps) puis la fonction retourne quand meme l'URL locale. localCoverURL (ArtworkLoader ligne 36) renvoie alors cette URL vers un fichier inexistant ; AsyncImage echoue silencieusement et l'UI reste s
  - *Fix proposé* : Faire `store` renvoyer `URL?` : `do { try data.write(to: url, options: .atomic); return url } catch { return nil }`, et cote appelant retomber proprement (renvoyer nil) quand la mise en cache echoue, plutot que de propager une URL fantome.
- **[performance]** `Sillon/Views/Shared/ArtworkLoader.swift:57` — coverURL ne memorise pas les resultats negatifs : re-resolution a chaque cellule
  - urlCache ne stocke qu'un hit positif (ligne 51). Quand un serveur ne renvoie pas de pochette (provider.coverArtURL == nil) ou qu'une erreur survient (catch ligne 54), la fonction renvoie nil sans rien memoriser. Pour les serveurs/chemins sans cover art, chaque reapparition de cellule reconstruit le 
  - *Fix proposé* : Memoriser aussi le resultat negatif, par ex. stocker `urlCache[key] = url` y compris quand url est nil (en passant la map en `[String: URL?]`), pour ne pas re-resoudre indefiniment les pochettes absentes.
- **[qualite]** `Sillon/Views/Shared/SourceBadge.swift:61` — Libelle d'accessibilite « N sources » sans gestion du singulier/pluriel ni localisation
  - accessibilityLabel("\(count) sources") affiche toujours « sources » au pluriel. SourceCountBadge n'apparait que pour count > 1 (cf. CoverArtView ligne 40), donc le pluriel est correct en pratique, mais la chaine est en dur, non localisee et n'utilise pas de format pluriel. Mineur, signale pour coher
  - *Fix proposé* : Utiliser une cle localisee avec pluriel automatique, ex. `Text("\(count) sources")` via une String catalog avec regle de pluralisation, ou au minimum un format explicite pour rester coherent avec l'i18n du projet.
